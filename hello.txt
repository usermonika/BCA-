Hello monika . How are you doing ?


git add. is used to merge 
git commit -m "addded then_sentence "
git config --global user.email "  "
git config --global user.name ""
git commit -m "added then_sentence "

git log (" it show the name and email ")

git add .
git status
git commit -m"added more questions "
git log
git add .
git commit -m "hi"
git log --oneline
git checkout code
git checkout master to go in first place.

git.com

new one 

git init
git add .
git commit -m"added Project Initalized"

git branch -M main
git status


Git hub
Create repository garney tes paxii name lekhney then description garna man lagey garney the create repository garney

To initalized the vs code project into github
Steps 
git add .
git init
git commit -m"added Project Initalized"
git status  (check the status )
git branch (to check which branch is )
git remote add origin https://github.com/usermonika/BCA-.git
git push -u origin main
